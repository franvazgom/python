Múltiplos de 3
================================================

Problema
--------

En el archivo `src\exercise.py` implementar una función con firma `multiplos_de_3(n:int)`:

* Debe devolver un conjunto con los múltiplos de 3 menores o iguales a `n`.
* `n` $\geq 0$.
* `0` es múltiplo de 3.
